@model Repozytorium.Models.Ogloszenie

@{
  ViewBag.Tytul = "Dodaj ogłoszenie";
  ViewBag.Opis = "Dodaj ogłoszenie - opis do Google";
  ViewBag.SlowaKluczowe = "Ogłoszenie, dodaj"; 
}

<h2>Dodaj nowe ogłoszenie</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Ogloszenie</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Tresc, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Tresc)
                @Html.ValidationMessageFor(model => model.Tresc)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Tytul, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Tytul)
                @Html.ValidationMessageFor(model => model.Tytul)
            </div>
        </div>

@*        <div class="form-group">
            @Html.LabelFor(model => model.DataDodania, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.DataDodania)
                @Html.ValidationMessageFor(model => model.DataDodania)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.UzytkownikId, "UzytkownikId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("UzytkownikId", String.Empty)
                @Html.ValidationMessageFor(model => model.UzytkownikId)
            </div>
        </div>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Dodaj" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Wróć", "Index", new {@class="btn btn-warning"})
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
